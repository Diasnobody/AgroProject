// Функция для появления и исчезновения навигационной панели
$(function() {
     $(window).scroll(function() {
          // Убираем логику добавления/удаления класса fixed-nav
          // var $this = $(window);

          // if($this.scrollTop() > 100) {
          //      $('#main-nav').addClass('fixed-nav');
          // } else {
          //      $('#main-nav').removeClass('fixed-nav');
          // }
     });
});